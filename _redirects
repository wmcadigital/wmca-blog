# Netlify Next.js routing configuration
# ISR (Incremental Static Regeneration) routes

# Preserve exact matches for pre-rendered pages
/article/:articleTitle 200!
/author/:authorName 200!
/demo-topics 200!
/ 200!

# Catch-all: ISR will handle on-demand generation
/* /:splat 200

